{
  "skill_name": "iqf-fe-schema-generator",
  "evals": [
    {
      "id": 1,
      "prompt": "V IQF projekte vygeneruj FE Zod schemas pre backend modul `pip-api/src/main/java/cz/inqool/udzs/pip/monitoring`. Najdi `@View` interfaces a DTO triedy, dodrz existujuci split `schemas/*.ts` layout vo frontend module a aktualizuj iba potrebne schema subory.",
      "expected_output": "Agent discovers backend `@View` interfaces through getter methods, finds matching frontend module, follows existing split schema layout, updates browse/detail/update schemas with lower-camel exports and local import style.",
      "files": [],
      "assertions": [
        "Reads JavaBean getter methods from `@View` interfaces, not only fields or classes.",
        "Follows the existing `schemas/*.ts` file layout instead of creating a new top-level `schema.ts`.",
        "Uses existing base schemas/import paths such as `domainSchema`, `baseSchema`, and project aliases where locally used."
      ]
    },
    {
      "id": 2,
      "prompt": "Aktualizuj existujucu FE update schemu pre monitoring podla aktualneho `MonitoringUpdateDto` a backend validatorov/sluzieb. Existujuca schema ma custom `.superRefine(...)` pravidla a lokalizovane hlasky; tie zachovaj len ak stale sedia na aktualny BE kontrakt, inak ich uprav alebo odstran.",
      "expected_output": "Agent updates DTO-backed fields, nullability, validation constraints, and FE-only cross-field `.superRefine(...)` rules so they match the current backend/form contract. Helper functions and localized messages are preserved when still valid, but changed or removed when backend behavior changed.",
      "files": [],
      "assertions": [
        "Updates or removes existing `.superRefine(...)` or `.refine(...)` logic when it no longer matches backend fields, validators, services, or form behavior.",
        "Keeps localized validation messages and helper functions only when they still describe the current backend/form behavior.",
        "Does not copy read-schema nullability or detail-view fields blindly into the DTO-backed update schema."
      ]
    },
    {
      "id": 3,
      "prompt": "Vygeneruj FormSchema/UpdateSchema pre novu IQF entitu, ktorej DetailSchema dedi z `domainSchema`. DTO obsahuje len editovatelne polia a id. Zachovaj lokalne konvencie pomenovania, ale rozhodni spravny base schema pre update/form schemu.",
      "expected_output": "Agent recognizes that form/update schemas model editable payloads and usually extend `baseSchema`, even when the read/detail schema extends `domainSchema`; it only uses more specific base schemas when local convention or DTO contract requires it.",
      "files": [],
      "assertions": [
        "Chooses `baseSchema` for `FormSchema`/`UpdateSchema` by default when consistent with local form/update schemas.",
        "Does not infer form/update base schema solely from entity inheritance or the detail schema base.",
        "Bases editable fields on `Dto`/`SaveDto`/`UpdateDto`, not on `DetailView`."
      ]
    },
    {
      "id": 4,
      "prompt": "Najdi alebo vytvor chybajuce FE schemas pre backend view, ktory referencuje view z ineho modulu, napriklad user browse/detail alebo case browse. Reuse existujuce schemas, ak uz v projekte su, a nevytvaraj duplicity.",
      "expected_output": "Agent resolves referenced backend views end-to-end, searches for existing FE schemas, imports and reuses them through local public aliases or relative paths, and reports missing referenced schemas instead of expanding scope unexpectedly.",
      "files": [],
      "assertions": [
        "Searches for matching existing FE schemas before creating referenced schemas.",
        "Reuses specific nested schemas instead of replacing references with generic `baseSchema` when a concrete view contract exists.",
        "Uses existing public import aliases or local relative import style consistently."
      ]
    },
    {
      "id": 5,
      "prompt": "Skontroluj a oprav FE schemas pre modul, kde backend view dedi z parent view/interface a DTO dedi zo superclass. Uisti sa, ze inherited getter/field cleny su v schemach spravne zachytene a ze validacie z parent DTO nie su stratene.",
      "expected_output": "Agent reads parent views/interfaces and DTO superclasses, composes or extends existing FE schemas when available, includes inherited serialized members, and carries inherited validation annotations into form/update schemas.",
      "files": [],
      "assertions": [
        "Reads inherited view interface methods and superclass fields/getters before finalizing read schemas.",
        "Includes inherited DTO validation annotations in form/update validation decisions.",
        "Composes or extends corresponding FE schemas when an inherited shape already exists."
      ]
    },
    {
      "id": 6,
      "prompt": "Backend kontrakt pre existujuci modul sa zmenil: jeden getter vo View bol odstraneny, pribudol novy nested view getter, DTO field zmenil nullable/required validaciu a enum field zmenil typ. Aktualizuj FE browse/detail/update schemas tak, aby BE bol source of truth, aj ked stare FE schemas maju ine polia a validacie.",
      "expected_output": "Agent treats backend as the source of truth, removes FE schema fields that no longer exist, adds new backend fields, updates nested schemas, enum/value schemas, nullability, requiredness, and form/update validation. Existing FE refinements are adjusted if they reference removed fields or enforce obsolete validation.",
      "files": [],
      "assertions": [
        "Removes FE schema fields that disappeared from the current backend view or DTO contract.",
        "Adds newly introduced backend fields and updates changed types, nested schemas, enum schemas, nullability, and requiredness.",
        "Updates `.refine(...)`/`.superRefine(...)` rules when referenced fields or backend validation semantics changed."
      ]
    }
  ]
}
